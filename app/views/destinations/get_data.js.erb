<% if flash.present? %>
  $("#flash-message").html("<%= escape_javascript(render partial: "shared/flash") %>");
<% else %>
  $("#items_grid").find('.row').html("<%= escape_javascript(render partial: "weather_cards/#{@weather[:type]}/card", locals: { weather: @weather } ) %>");
<% end %>
